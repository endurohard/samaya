-- 036_booking_color.sql
-- Цвет записи: администраторы помечают цветом тип визита (первичный, повторный,
-- проблемный клиент) и читают календарь не вчитываясь в текст карточек.
-- У «занятого времени» такой цвет уже есть — приводим записи к тому же виду.

SET search_path TO bookings;

ALTER TABLE bookings
  ADD COLUMN IF NOT EXISTS color TEXT;
